#############################################
# Base / Shared Configuration
#############################################

# External Docker network both host & agent services can join.
SHARED_DOCKER_NETWORK=cloudflared_shared-network

#############################################
# Host (Portainer Server) Mode
#############################################
# Exposed host port for the Portainer UI (container port 9000)
PORTAINER_PORT=9000

#############################################
# Edge Agent (Standalone) Mode
#############################################
# Host port mapping for the Edge Agent (container 9001). Change if 9001 conflicts.
PORTAINER_AGENT_PORT=9001

# Set ONLY when running Edge Agent mode (docker-compose-edge-agent.yml)
# Obtain these from: Portainer UI -> Environments -> Add Environment -> Edge Agent
PORTAINER_EDGE_ID=<replace_with_edge_id>
PORTAINER_EDGE_KEY=<replace_with_edge_key>

# (Optional) Allow insecure (non-TLS) polling to Edge endpoint (development only). Set to 1 to enable.
PORTAINER_EDGE_INSECURE_POLL=0

# (Optional) Agent log level: DEBUG | INFO | WARN | ERROR
PORTAINER_LOG_LEVEL=DEBUG

#############################################
# Notes
# - Leave EDGE_INSECURE_POLL at 0 when your Edge URL uses HTTPS.
# - Never commit real EDGE_ID / EDGE_KEY to public repositories.
# - If running ONLY the server, EDGE_* variables are ignored.
#############################################
